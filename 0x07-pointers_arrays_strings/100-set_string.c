#include "main.h"

/**
 * set_string - function sets the value of pointer to char
 * @s: first parameter
 * @to: second parameter
 * Returns: always 0
 */

void set_string(char **s, char *to)
{
*s = to;
}
